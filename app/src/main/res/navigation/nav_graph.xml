<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainMenuFrag">

    <fragment
        android:id="@+id/mainMenuFrag"
        android:name="dk.dtu.rokas.hangman.fragment.MainMenuFrag"
        android:label="main_menu"
        tools:layout="@layout/main_menu" >
        <action
            android:id="@+id/action_mainMenuFrag_to_gameFrag"
            app:destination="@id/gameFrag" />
    </fragment>
    <fragment
        android:id="@+id/gameFrag"
        android:name="dk.dtu.rokas.hangman.fragment.GameFrag"
        android:label="game"
        tools:layout="@layout/game" />
</navigation>